---
name: athena
license: MIT
description: 通过可扩展的命名风格目录生成一致、可复用且经过质量验收的位图。用户调用 $athena、要求查看或选择生图风格、指定 style-id 或中文风格名、要求延续既有二维或三维系列画风、只描述主体但希望自动保持固定视觉语言，或需要控制尺寸、构图、裁切、精确文字、文件保存和成图验收时使用。当前支持 marker-doodle、scribble-ink、soft-clay 与 knit-doll；未指定风格时必须先展示可用风格菜单。仅用于生成新位图；不要用于视频、SVG、HTML/CSS 绘图或现有图片的精确局部编辑。
---

# Athena

把用户的内容需求与一份明确的风格规范组合成最终提示词，再通过统一的生成、保存和验收流程输出图片。Athena 不是单一画风；它负责选择并隔离风格、执行生图，以及验证最终结果。每次只加载本次选中的风格文件。

## 调用格式

优先识别以下格式：

```text
使用 $athena，风格：marker-doodle，生成：一个男性坐在沙发上使用电脑。
```

用户也可以只打开风格菜单：

```text
使用 $athena，请列出可用风格让我选择。
```

也接受中文名、已登记别名或自然语言，例如：

```text
使用 $athena，用“黑白粗马克笔”风格画一个女孩牵狗散步，横版，不要文字。
```

## 风格路由

| style-id | 用户看到的名称 | 一眼看懂 | 别名 | 风格文件 |
|---|---|---|---|---|
| `marker-doodle` | 黑白粗马克笔涂鸦 | 极简粗黑线、纯黑白、完整主体和大量留白，适合独立编辑插图与后期排版 | 马克笔涂鸦、黑白马克笔、粗线涂鸦、黑白粗马克笔 | [references/marker-doodle.md](references/marker-doodle.md) |
| `scribble-ink` | Scribble Ink 涂鸦墨线 | 松散黑色毡笔线、夸张人物半身像、纯白背景和少量珊瑚粉点缀 | 涂鸦墨线、乱笔墨线、毡笔人物、珊瑚粉涂鸦、Scribble Ink | [references/scribble-ink.md](references/scribble-ink.md) |
| `soft-clay` | soft-clay | 单主体、低细节、原始圆润体块、小点眼和单色柔和摄影棚背景，适合系列化角色与 App 视觉 | 柔光黏土、治愈黏土、马卡龙黏土、粘土卡通、Claymation | [references/soft-clay.md](references/soft-clay.md) |
| `knit-doll` | knit-doll | 平滑玩偶脸、刷毛毛毡针织、硬质微缩鞋，以及随机选择的明亮童话纤维世界 | 针织玩偶、毛线玩偶、羊毛毡玩偶、纤维玩偶、Cozy Knit Doll | [references/knit-doll.md](references/knit-doll.md) |

按以下规则选择风格：

1. 用户给出 style-id、用户看到的名称或唯一别名时，归一为正式 style-id。
2. 用户调用 Athena 但未指定风格时，先展示下面的风格菜单并等待选择；不得随机选择，也不得只说“请指定风格”而不列出选项。
3. 用户说“任选”或“帮我选”时，根据题材选择，并在生成前说明选择结果。
4. 默认只使用一种主风格。只有用户明确要求混合时才接受 `主风格：<id>；辅助风格：<id>；比例：80/20`；分别读取两份文件并以主风格控制构图与整体视觉。
5. 选择完成后，完整读取对应风格文件；不要读取未选中的风格文件。

未指定风格时使用以下简短菜单：

```text
请选择一种 Athena 风格：

1. marker-doodle｜黑白粗马克笔涂鸦
   极简粗黑线、纯黑白、完整主体和大量留白。

2. scribble-ink｜Scribble Ink 涂鸦墨线
   松散黑色毡笔线、夸张人物半身像、纯白底和少量珊瑚粉点缀。

3. soft-clay
   单主体、低细节圆润黏土玩具、小点眼和柔和单色摄影棚背景。

4. knit-doll
   平滑玩偶脸、刷毛毛毡针织、硬质鞋和随机明亮童话世界。

回复编号、style-id 或中文名称，并告诉我想画什么；也可以说“帮我选”。
```

## 工作流

1. 提取主体、动作、必需对象、用途、画幅或尺寸、精确文字、背景要求、参考图角色及禁止项。不要添加用户没有要求的标语、人物、道具、品牌或场景；但若所选风格文件明确登记了默认背景布景语法，可加入其中限定的低清晰度环境陈设与无文字装饰，仍不得增加新人物、主要角色、品牌或改变用户叙事。
2. 解析风格并只加载对应的 `references/<style-id>.md`。
3. 将用户要求、风格文件中的视觉规范及构图约束编译为一份结构化最终提示词。用户明确要求优先，但不得悄悄放宽风格规则；若要求与风格的定义性特征直接冲突，先用一个简短问题确认。
4. 按“生图执行层选择”使用当前会话可用的新位图生成能力。使用已选执行层对应的提示词、尺寸、质量、保存和验收规范；不要假设某个网关、模型或 skill 一定存在。
5. 根据下方规则选择合法画布和质量。能直接写入本地文件的执行层保存到当前项目的 `output/imagegen/`，使用语义化且不覆盖已有文件的 `.png` 文件名；只能返回内联预览的执行层则如实说明未提供本地文件路径。
6. 用 `view_image` 检查成图。验证内容完整性、所选风格、构图、裁切、颜色、精确文字和禁止项；同时检查风格文件中的专属验收标准。
7. 如未通过，只做一次针对失败项的重试，不要在重试时改变已正确的内容。仍未通过时如实说明残留偏差，不得把不合格结果描述为已完成。
8. 返回内联预览、可用的绝对保存路径、最终提示词、style-id、实际尺寸、执行层，以及网关与模型（如适用）；若风格选择了命名背景预设，同时返回本次使用的 preset-id。

## 生图执行层选择

默认原则是“有啥用啥”，但遵守三条边界：

1. 用户、本机、项目或上层指令本次明确指定某个网关、API、脚本、模型或工具时，使用指定执行层。
2. 没有明确指定时，选择当前会话里已经可用、能完成新位图生成的执行层，例如 `ddit-image-generation`、`imagegen` / `image_gen`，或用户配置的其他图片网关；不要为了偏好某个工具而跳过可用能力。
3. 若没有任何可用的新位图生成能力，停止并说明缺少生图执行层；不要假装已经生成图片，也不要输出无法验证的路径。

执行层如果能直接写本地文件，保存并返回绝对路径；如果只能返回会话内预览而不暴露文件路径，正常交付预览并如实说明没有本地文件路径。

## 通用尺寸与输出规则

- 用户给出有效精确尺寸时优先使用；只给比例或方向时，选择接近该比例的合法画布；两者都未给出时使用风格文件的默认画布。
- 若执行层或模型支持自定义宽高，必须遵守其尺寸限制。使用 DDIT / `gpt-image-2` 时，自定义宽高必须都是 16 的倍数，单边不超过 3840 px，长短边之比不超过 3:1，总像素数须在 655,360–8,294,400 之间。
- 常用合法尺寸：`1024x1024`、`1536x1024`、`1024x1536`、`2048x2048`、`2048x1152`、`3840x2160`、`2160x3840`。风格文件可以登记其他合法默认尺寸。
- 精确目标尺寸不受模型支持时，先生成同画幅或最接近画幅的合法母版，再等比缩放或增加与背景一致的留白。禁止裁切主体，禁止非等比拉伸。
- 默认质量为 `medium`；最终交付、精确文字、复杂细节或用户要求高质量时使用 `high`。
- 默认生成一个结果。只有用户明确要求变体或批量时才生成多个，并为每个文件使用独立语义化名称。
- 不得覆盖已有文件，除非用户明确要求替换；否则使用 `-v2`、`-v3` 等后缀。
- 需要透明背景且主体边缘简单时，先按 `imagegen` 的纯色键控背景流程生成并本地去背，再验证 alpha 与边缘。复杂毛发、烟雾、玻璃或半透明材质不得承诺原生透明结果。

## 提示词组装顺序

只保留与请求有关的字段：

```text
Use case: <用途分类>
Asset type: <使用场景与交付类型>
Primary request: <用户的主体与动作>
Required objects: <仅列明确要求的对象>
Scene/backdrop: <背景>
Style/medium: <从风格文件载入>
Subject design: <从风格文件载入>
Composition/framing: <用户要求 + 风格构图规则>
Lighting/mood: <如适用>
Color palette: <用户要求 + 风格色彩规则>
Materials/textures: <如适用>
Text (verbatim): <精确文字；没有则写 no text>
Constraints: <必须保持的内容>
Avoid: <用户禁止项 + 风格禁止项>
```

不要把内部文件名、style-id、尺寸 API 规则或操作说明当作画面文字交给模型绘制。

## 能力边界

- 本 Skill 用于生成新的位图，不负责视频、SVG、HTML/CSS 绘图或现有图片的精确局部编辑。
- 参考图可以用于说明主体、构图或风格，但不得假装获得了参考图中不可见的信息。
- 风格规范提高一致性，不保证生成模型一次就能正确绘制复杂文字、手部或密集小物；必须依靠验收和定向重试处理。

不适用反例：“把这张照片里的人替换成另一个人，同时保证其余像素完全不变。”这是精确局部编辑任务，不由 Athena 处理。

## 使用示例

- `使用 $athena，请先列出可用风格让我选择。`
- `使用 $athena，风格：marker-doodle，生成：一个男性坐在沙发上使用电脑。`
- `使用 $athena，风格：scribble-ink，生成：一位戴圆框眼镜、微笑的女性程序员，不要文字。`
- `使用 $athena，风格：soft-clay，生成：一只抱着咖啡杯的小熊，奶油色背景，不要文字。`
- `使用 $athena，风格：knit-doll，生成：一个戴圆框眼镜、拿着手机大笑的年轻人，竖版，不要文字。`
- `使用 $athena，帮我从现有风格里选择一个适合画人物头像的风格：一位留胡子的中年男性抱着一只猫。`
